#Name of the workflow
name: Code

#when the workflow should run
on:
  push:

#jobs in the workflow
jobs:
   code-checkout:
      runs-on: ubuntu-latest
      steps:
        - name: Clone the code
          uses: actions/checkout@v7

        - name: Print the message
          uses: echo "Done with your 1st Actions"
        
